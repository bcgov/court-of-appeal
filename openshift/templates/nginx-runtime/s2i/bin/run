#!/bin/bash

#sed -e "s/\$API_URL/${API_URL}/" ../../nginx.conf.template 

sed -e "s/\$API_URL/${API_URL}/" /tmp/nginx.conf.template > /etc/nginx/nginx.conf
cat /etc/nginx/nginx.conf

echo "---> Starting nginx ..."
/usr/sbin/nginx -g "daemon off;"